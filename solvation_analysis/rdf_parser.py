import numpy as np
import numpy.polynomial.polynomial
import matplotlib.pyplot as plt


def quick_plot(bins, rdf):
    plt.plot(bins, rdf, 'bo')
    plt.show()


def identify_minima(rdf, bins, n_minima):

    return
