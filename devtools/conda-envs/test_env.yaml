name: test
channels:

  - conda-forge

  - defaults
dependencies:
    # Base depends
  - python
  - pip
  - scipy
  - numpy
  - pandas
  - matplotlib
  - pathlib

    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Pip-only installs
  - pip:
      - MDAnalysis==2.0.0b0

