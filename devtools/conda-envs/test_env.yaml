name: test
channels:
  - conda-forge
  - defaults
dependencies:
    # Base depends
  - python
  - pip

    # Pip-only installs
  - pip:
      # new MDAnalysis
      - MDAnalysis==2.3.0
      # base sci-python libraries
      - scipy
      - numpy
      - pandas
      - matplotlib
      - statsmodels
      # testing #
      - pytest
      - pytest-cov
      - codecov
      - rdkit
      - plotly