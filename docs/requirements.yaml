name: docs
channels:
  - conda-forge
  - defaults
dependencies:
    # Base depends
  - python=3.12
  - pip
  - numpy>=2.2
  - pytest
  - mdanalysis>=2.8
  - pandas>=2.2
  - nglview
  - nbsphinx
  - ipython
  - plotly
  - sphinx_rtd_theme
  - scipy


    # Pip-only installs
  #- pip:
