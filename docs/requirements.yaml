name: docs
channels:
  - conda-forge
  - defaults
dependencies:
    # Base depends
  - python
  - pip
  - numpy
  - pytest
  - mdanalysis
  - nglview
  - nbsphinx
  - ipython
  - plotly
  - sphinx_rtd_theme


    # Pip-only installs
  #- pip:

